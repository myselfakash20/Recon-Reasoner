from recon_reasoner.cli import app

if __name__ == "__main__":
    app()
# This is the main entry point for the Recon Reasoner CLI application.
# It imports the CLI app from the `cli` module and runs it when this script is executed.
# The CLI app is built using Typer, which provides a user-friendly command-line interface
# for running the Recon Reasoner tool.  